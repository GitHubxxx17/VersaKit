import React from "react";

import { Button } from "@/index";

const App: React.FC = () => {
  return (
    <>
      <Button size="large">按 钮</Button> &nbsp;
      <Button size="default">按 钮</Button> &nbsp;
      <Button size="small">按 钮</Button>
    </>
  );
};

export default App;
